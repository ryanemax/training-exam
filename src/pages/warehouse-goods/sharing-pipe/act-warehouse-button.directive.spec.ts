import { ActWarehouseButtonDirective } from './act-warehouse-button.directive';

describe('ActWarehouseButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ActWarehouseButtonDirective();
    expect(directive).toBeTruthy();
  });
});
