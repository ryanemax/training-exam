import { ActTrDirective } from './act-tr.directive';

describe('ActTrDirective', () => {
  it('should create an instance', () => {
    const directive = new ActTrDirective();
    expect(directive).toBeTruthy();
  });
});
