import { ActCardDirective } from './act-card.directive';

describe('ActCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ActCardDirective();
    expect(directive).toBeTruthy();
  });
});
