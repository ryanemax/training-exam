import { AsThousandPipe } from './as-thousandPipe.pipe';

describe('AsThousandPipe', () => {
  it('create an instance', () => {
    const pipe = new AsThousandPipe();
    expect(pipe).toBeTruthy();
  });
});
