import { InvButtonDirective } from './inv-button.directive';

describe('InvButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new InvButtonDirective();
    expect(directive).toBeTruthy();
  });
});
