import { ActButtonDirective } from './act-button.directive';

describe('ActButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ActButtonDirective();
    expect(directive).toBeTruthy();
  });
});
